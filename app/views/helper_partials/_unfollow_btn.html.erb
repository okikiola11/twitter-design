 <%= form_for(current_user.active_followings.find_by(followed_id: @user.id),
  html: { method: :delete }) do |f| %>
  <%= f.button'<i class="fas fa-minus-circle fa-2x"></i>'.html_safe, class: "btn" %>
<% end %>